import React, { Component } from "react";
import store from "./store";

/**
 * - Agregar `redux` como dependencia
 * - Dentro del archivo `store.js`, crear y exportar un store que sea un contador
 *   (basado en https://redux.js.org/introduction/getting-started#basic-example)
 * - Mostrar el estado de ese store en el `render`
 */

class App extends Component {
  render() {
    return <div />;
  }
}

export default App;
